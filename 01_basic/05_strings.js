const name = "Himesh"
const repoCount = 50

console.log(`Hello the name is ${name} and total repos are ${repoCount}`);

const gameName = new String('Need For Speed')

console.log(gameName.toUpperCase())
console.log(gameName.length)
console.log(gameName.charAt(3))